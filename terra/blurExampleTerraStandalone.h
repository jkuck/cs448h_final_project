extern "C"

void terraFuncNameInC3(float* inputImg, float* inputVar, unsigned short * inputMask,
					float* outputImg, float* outputVar, unsigned short * outputMask,
                    int imageWidth, int imageHeight, float** kernelArray, 
                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
                    int kernelHeight, int numberOfFuncCoef);

//void terraFuncNameInC5(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);

//void terraFuncNameInC7(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);
//
//void terraFuncNameInC9(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);
//
//void terraFuncNameInC11(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);
//
//void terraFuncNameInC13(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);
//
//void terraFuncNameInC15(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);
//
//void terraFuncNameInC17(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);
//
//void terraFuncNameInC19(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);
//
//void terraFuncNameInC21(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);
//
//void terraFuncNameInC23(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);
//
//void terraFuncNameInC25(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);
//
//void terraFuncNameInC27(float* inputImg, float* inputVar, unsigned short * inputMask,
//					float* outputImg, float* outputVar, unsigned short * outputMask,
//                    int imageWidth, int imageHeight, float** kernelArray, 
//                    float* funcParams, int numberOfBasisKernels, int kernelWidth,
//                    int kernelHeight, int numberOfFuncCoef);
//
//